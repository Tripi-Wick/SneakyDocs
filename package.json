{
  "name": "collapse-docs",
  "displayName": "Collapse Docs",
  "description": "Quickly collapse or uncollapse all docstrings and JSDoc comments via right-click menu.",
  "publisher": "Oskarlypka",
  "version": "1.4.0",
  "engines": {
    "vscode": "^1.60.0"
  },
  "activationEvents": [
    "onLanguage:python",
    "onLanguage:javascript",
    "onLanguage:typescript"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "collapseDocs.run",
        "title": "Collapse All Docs"
      },
      {
        "command": "collapseDocs.unfold",
        "title": "Uncollapse All Docs"
      },
      {
        "command": "collapseDocs.exportFunction",
        "title": "Update Module Export"
      },
      {
        "command": "collapseDocs.generateDocs",
        "title": "generate Docs"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "collapseDocs.run",
          "when": "editorLangId == javascript || editorLangId == typescript || editorLangId == python",
          "group": "collapseDocsGroup@1"
        },
        {
          "command": "collapseDocs.unfold",
          "when": "editorLangId == javascript || editorLangId == typescript || editorLangId == python",
          "group": "collapseDocsGroup@2"
        },
        {
          "command": "collapseDocs.exportFunction",
          "when": "editorLangId == javascript || editorLangId == typescript",
          "group": "collapseDocsGroup@3"
        },
        {
          "command": "collapseDocs.generateDocs",
          "when": "editorLangId == javascript || editorLangId == typescript",
          "group": "collapseDocsGroup@4"
        }
      ]
    }
  },
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "icon": "images/icon.png",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "vscode": "^1.1.37"
  }
}
